import React from 'react';

const Portfolio = () => {
  return (
    <div style={{ padding: 'clamp(0.75rem, 2.5vw, 2rem)' }}>
      <h2 style={{ fontSize: 'clamp(1.25rem, 3vw, 2rem)', marginBottom: 'clamp(0.5rem, 2vw, 1rem)' }}>Portfolio</h2>
      <p style={{ fontSize: 'clamp(0.95rem, 2.2vw, 1.1rem)', lineHeight: 1.6 }}>This page will display selected works and exhibitions.</p>
    </div>
  );
};

export default Portfolio;
